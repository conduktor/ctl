apiVersion -> version
get prend ou deux argument il remplace le describe
-d -> -v 
-k (-R pour récursion dans les sous dossier)
get il faut du yaml equivalent à ce qui est envoyé

apply/delete resultat effective: 
  application/yo: (CREATED)
  application/yo: (UPDATED)
  application/yo: (UNCHANGED)

dry-run
audit log for CRUD
diff get/comparaison des yaml
